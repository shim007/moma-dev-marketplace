---
name: moma-retro
description: 'moma-soft-dev 子技能 —— 项目复盘。当用户消息以 /moma-retro 开头时使用本技能， 对项目或里程碑进行复盘：聚合会话归档、变更日志、缺陷记录与版本历史，生成复盘报告 （目标达成、过程数据、经验教训、可落地的改进行动）到 retro-reports/ 目录。 本指令不修改代码与 01~07 文档。'
---

# /moma-retro —— 项目复盘

里程碑或项目级复盘：聚合过程数据、提炼经验教训、产出可落地的改进项。与会话级的 `/moma-session-complete` 互补——后者记录单次会话，本指令跨越多个会话与版本做全局回顾。

> 共享约定见 `${CLAUDE_PLUGIN_ROOT}/skills/moma-soft-dev/references/common-conventions.md`。本文件中 `dev-docs/` 路径均遵循共享约定的文档存放规则。

## 上下文范围

加载复盘范围内的 `<docs-root>/session-records/` 会话归档 + `dev-docs/08-变更日志.md` + `dev-docs/09-BUG记录.md` + `<docs-root>/releases/` 发布记录 + `dev-docs/01-项目概要.md`（初始目标对照）+ git 统计（提交数与时间线）。不加载插件内置模板与业务代码。

## 步骤

1. **登记**
   - 按"多会话并行规约"生成会话 ID，读取花名册 `<docs-root>/.moma-state/roster.md`（不存在则创建），登记本会话条目（指令 `/moma-retro`、工作范围"只读复盘 + retro-reports/"、状态"进行中"）。
   - 本指令不修改代码、不修改 01~07 文档，不构成工作范围冲突，可与其他指令并行；无需 checkpoint 提交。
2. **确定复盘范围**：整个项目，或用户指定的里程碑/版本区间（如"v1.0.0 到 v1.2.0"、"自上次复盘以来"）。区间不明确时询问用户。
3. **聚合过程数据**（某类数据源不存在时跳过该项并在报告中注明，不中止）：
   - **会话归档**：会话数量、`/moma-*` 指令使用分布、关键决策清单（`session-records/` 不存在时注明"无会话归档数据"）；
   - **变更日志**：CR 数量与类型分布（需求变更 / 设计变更 / 技术变更 / 缺陷修复 / 事故应急 / 数据模型变更）；
   - **缺陷记录**：BUG 数量、级别分布、当前未关闭（待修复/修复中）数量与清单；
   - **版本历史**：发布次数、发布间隔、各版本亮点与已知问题；git 提交统计在项目无版本控制时跳过并注明。
4. **生成复盘报告**：写入 `<docs-root>/retro-reports/retro-YYYYMMDD-<主题>.md`（主题 kebab-case），使用文末模板。要求：
   - **目标达成对照**：以 01-项目概要的初始目标为基准，逐项标注已达成/部分达成/未达成及原因；
   - **经验与教训**：每条教训按"问题 → 原因 → 教训"结构书写，避免空泛总结；
   - **改进行动**：每条必须可落地，并标注承接指令——流程规范类 → `/moma-modify` 更新 06-开发规约；测试缺口 → `/moma-test`；遗留缺陷 → `/moma-fix`；架构风险 → `/moma-optimize`；文档漂移 → `/moma-check`。
5. **登记**：按追加式写入规则在 `dev-docs/08-变更日志.md` 追加一条 CR 条目（变更类型"项目复盘"，引用复盘报告文件，条目含会话 ID；文件不存在则创建）。
6. **收尾**：将花名册中本会话状态改为"已完成"；向用户呈现优先级最高的前 3 项改进行动，并建议立即执行对应指令。

## 复盘报告模板

```markdown
# 复盘报告：{{主题}}

- **会话 ID：** {{会话 ID}}
- **复盘范围：** {{整个项目 / 版本区间 vX.Y.Z ~ vX.Y.Z}}
- **复盘日期：** {{YYYY-MM-DD}}

## 1. 目标达成对照

| 初始目标（01-项目概要） | 状态 | 说明 |
|--------------------------|------|------|
| | 已达成/部分达成/未达成 | |

## 2. 过程数据

- 会话归档：{{N}} 次；指令分布：{{统计}}
- 变更：CR 共 {{N}} 条（{{类型分布}}）
- 缺陷：BUG 共 {{N}} 条（{{级别分布}}），未关闭 {{N}} 条
- 发布：{{N}} 个版本，平均间隔 {{周期}}

## 3. 做得好的地方

- {{经验，说明为什么有效、如何保持}}

## 4. 问题与教训

### {{问题标题}}
- **问题：**
- **原因：**
- **教训：**

## 5. 改进行动

| # | 行动 | 承接指令 | 优先级 |
|---|------|----------|--------|
| 1 | | /moma-* | 高/中/低 |
```

此指令只做回顾与总结，不修改代码与 01~07 文档；所有改进落地都通过建议指令由用户发起。
