---
name: moma-optimize
description: >
  moma-soft-dev 子技能 —— 功能优化。当用户消息以 /moma-optimize 开头时使用本技能，
  对已有需求进行更详细的约束和优化（细化现有功能的行为边界，而非新增功能），同步更新 01/02 及受影响的下游文档，
  按需进行代码变更，在 08-变更日志.md 中登记，并以 optimize: 前缀格式生成版本控制提交。
---

# /moma-optimize —— 功能优化

对已有需求进行更详细的约束和优化。这通常是细化现有功能的行为边界，而非新增功能。

> 共享约定见 `../moma-soft-dev/references/common-conventions.md`，编码通用约束必须严格遵守。

## 上下文范围

与 `/moma-modify` 相同 —— 加载用户输入 + 01/02 文档 + 受影响的下游文档 + 变更日志 + 相关代码。不加载模板和无关内容。

## 步骤

1. 结合 01 和 02 文档，分析用户输入中哪些需求细节需要调整。
2. 更新阶段1文档，并同步更新受影响的下游文档（03~07）。如果下游文档已存在，必须逐一检查并更新；如果尚未生成则跳过。
3. 根据变更需求进行代码变更，遵循 `⚠️ AI 约束`。
4. 在 `docs/08-变更日志.md` 中记录本次变更（不存在则创建）。
5. 如果项目有版本控制，生成 `optimize: <变更简述>` 格式的提交。
