---
name: moma-sync
description: 'moma-soft-dev 子技能 —— 项目同步 / 需求同步。当用户消息以 /moma-sync 开头时使用本技能， 针对已有代码的项目，通过分析现有代码反向补全或生成 01~07 文档体系，让后续的 AI 编码也能受到文档约束。 本指令仅生成/补全文档，不直接修改代码。'
---

# /moma-sync —— 项目同步 / 需求同步

针对已有代码的项目，通过分析现有代码反向补全或生成文档体系。

> 共享约定见 `../moma-soft-dev/references/common-conventions.md`，本文件中 `docs/` 路径均遵循 `dev-docs/` 优先规则。

## 上下文范围

加载当前工作目录下的项目文件（源码、配置、构建脚本等）+ `simple-software/` 目录下的模板（01~07）+ 已存在的 `dev-docs/` 或 `docs/` 文档（如有）。不加载变更日志（此时通常无意义）。

## 步骤

1. **分析项目**
   - 扫描当前目录下的源码、配置、依赖清单等文件，整理出项目的技术栈和关键技术细节（语言、框架、数据库、构建工具、目录结构等）。
   - 根据项目已实现的功能反向推导出对应的需求，并缓存以供后续阶段使用。

2. **判断文档状态并执行**
   - **如果文档文件夹中已存在"阶段1"对应的文档**（即 `01-项目概要.md` 或 `02-产品需求文档.md`）：使用上一步分析得到的需求对已有文档进行补充和完善，然后自动执行 `/moma-init` 中的阶段2、阶段3 流程，补齐缺失文档。
   - **如果文档文件夹尚未创建或阶段1文档缺失**：从阶段1开始，按照 `/moma-init` 的完整流程生成全套文档（阶段1→2→3），生成时以分析得到的需求和技术细节为输入。

3. **完成后提示用户**
   - 请用户审核所有文档内容，确认与代码现状一致。
   - 建议用户发送 `/moma-coding` 进入阶段4，后续编码即受文档约束。

此指令不直接修改代码，仅生成/补全文档。
