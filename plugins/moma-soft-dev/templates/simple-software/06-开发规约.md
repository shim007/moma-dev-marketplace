# 开发规约

> **定位：** AI 编写每一行代码时必须遵守的规范和约束集合，是最直接的"AI 行为准则"。
> **生成时机：** 技术架构文档确认后即可生成，与数据模型和 API 文档可并行。
> **使用方式：** 在每次 AI 编码会话开始时，将本文档作为系统级上下文注入，确保 AI 全程遵守。

---

## 1. 文档信息

| 字段 | 内容 |
|------|------|
| 项目名称 | {{项目名称}} |
| 版本 | V1.0 |
| 关联文档 | [技术架构文档](./03-技术架构文档.md) |

---

## 2. 通用编码规范

### 2.1 命名规范

> **⚠️ AI 约束：以下命名规范是强制性的，AI 不得使用其他命名风格。**

| 元素 | 规范 | 示例 |
|------|------|------|
| 项目/仓库名 | <!-- AI_FILL: 如 kebab-case --> | `my-project` |
| 文件名 | <!-- AI_FILL --> | |
| 类名 | <!-- AI_FILL: 如 PascalCase --> | `UserService` |
| 函数/方法名 | <!-- AI_FILL: 如 camelCase --> | `getUserById` |
| 变量名 | <!-- AI_FILL --> | |
| 常量名 | <!-- AI_FILL: 如 UPPER_SNAKE_CASE --> | `MAX_RETRY_COUNT` |
| 数据库表名 | <!-- AI_FILL: 如 snake_case --> | `user_order` |
| 数据库字段名 | <!-- AI_FILL --> | |
| API 路径 | <!-- AI_FILL: 如 kebab-case --> | `/user-orders` |
| 环境变量 | <!-- AI_FILL --> | `DATABASE_URL` |

### 2.2 代码风格

<!-- AI_FILL: 缩进（空格/Tab）、行宽限制、引号风格、分号等基础风格约定 -->

### 2.3 注释规范

<!-- AI_FILL: 何时需要注释、注释格式（如 JSDoc/Javadoc/docstring）、禁止的注释类型 -->

**AI 特别约束：**
- ✅ 复杂业务逻辑必须添加注释
- ✅ 公共 API/函数必须有文档注释
- ❌ 禁止无意义的注释（如 `// 设置名字` `this.name = name`）
- ❌ 禁止注释掉的代码提交

---

## 3. 架构规范

### 3.1 分层职责

> **⚠️ AI 约束：严格遵守分层架构，不得跨层调用。**

<!-- AI_FILL: 根据技术架构文档定义各层的职责和调用规则 -->

| 层级 | 职责 | 可以调用 | 不可调用 |
|------|------|----------|----------|
<!-- REPEAT_START -->
| {{层级}} | {{职责}} | {{允许调用的层}} | {{禁止调用的层}} |
<!-- REPEAT_END -->

### 3.2 模块通信规则

<!-- AI_FILL: 模块间通信方式，如通过接口/事件/消息队列等 -->

---

## 4. 错误处理规范

> **⚠️ AI 约束：AI 编码时必须按以下规范处理错误，不得静默吞掉异常。**

### 4.1 错误处理原则

<!-- AI_FILL: 错误分类（业务异常/系统异常）、处理策略、日志记录要求 -->

### 4.2 统一异常结构

```
// AI_FILL: 自定义异常类/错误结构的代码示例
```

### 4.3 日志规范

| 日志级别 | 使用场景 | 示例 |
|----------|----------|------|
| ERROR | <!-- AI_FILL --> | |
| WARN | <!-- AI_FILL --> | |
| INFO | <!-- AI_FILL --> | |
| DEBUG | <!-- AI_FILL --> | |

---

## 5. 安全编码规范

> **⚠️ AI 约束：以下安全规范为红线，违反任一条即视为严重缺陷。**

- [ ] 所有用户输入必须校验和净化
- [ ] SQL 查询必须使用参数化查询，禁止拼接
- [ ] 敏感信息（密码、密钥）禁止硬编码，必须使用环境变量
- [ ] 密码存储必须使用加盐哈希
- [ ] API 必须有速率限制
- [ ] 文件上传必须校验类型和大小
<!-- AI_FILL: 补充项目特定的安全规范 -->

---

## 6. 版本控制规范

### 6.1 分支策略

<!-- AI_FILL: 分支命名规范和工作流（如 Git Flow / Trunk Based） -->

### 6.2 提交信息规范

> **⚠️ AI 约束：每次提交必须遵循以下格式。**

```
<type>(<scope>): <subject>

<body>

<footer>
```

**type 取值：**

| 类型 | 说明 |
|------|------|
| feat | 新功能 |
| fix | 修复缺陷 |
| docs | 文档变更 |
| style | 代码格式（不影响逻辑） |
| refactor | 重构 |
| test | 测试相关 |
| chore | 构建/工具变更 |

---

## 7. 依赖管理规范

- **添加新依赖前**必须确认：
  - [ ] 该依赖在技术架构文档中已列出，或已获得确认更新架构文档
  - [ ] 许可证兼容性已确认
  - [ ] 无已知高危漏洞
- **禁止事项：**
  - ❌ 禁止引入与已有依赖功能重复的库
  - ❌ 禁止使用已废弃（deprecated）的库
  - ❌ 禁止锁定不明确的版本范围（如 `*` 或 `latest`）

---

## 8. 测试规范

### 8.1 测试要求

<!-- AI_FILL: 单元测试覆盖率要求、必须测试的场景 -->

### 8.2 测试命名规范

<!-- AI_FILL: 测试文件和测试用例的命名规范 -->

### 8.3 测试目录结构

```
<!-- AI_FILL: 测试代码的组织结构 -->
```

---

## 9. AI 编码专项约束

> **以下约束专门针对 AI 代码生成场景设计，防止 AI 常见的编码偏差。**

### 9.1 禁止行为清单

| 编号 | 禁止行为 | 说明 |
|------|----------|------|
| R-01 | 禁止自行添加需求文档中未定义的功能 | 防止范围蔓延 |
| R-02 | 禁止更换技术架构文档中确定的技术栈 | 保持技术一致性 |
| R-03 | 禁止添加数据模型文档中未定义的表和字段 | 保持数据结构一致 |
| R-04 | 禁止修改 API 文档中已定义的接口契约 | 保持接口稳定 |
| R-05 | 禁止使用 `any` 类型（强类型语言项目） | 保证类型安全 |
| R-06 | 禁止在代码中硬编码配置值 | 使用配置文件/环境变量 |
| R-07 | 禁止忽略错误处理 | 所有错误必须捕获和处理 |
| R-08 | 禁止生成占位符代码（如 `// TODO: implement`） | 必须实现完整逻辑 |
<!-- REPEAT_START -->
| R-{{编号}} | {{禁止行为}} | {{说明}} |
<!-- REPEAT_END -->

### 9.2 必须行为清单

| 编号 | 必须行为 | 说明 |
|------|----------|------|
| M-01 | 新增功能必须同步编写单元测试 | 保证可测试性 |
| M-02 | 每个文件头部必须有模块说明注释 | 提高可读性 |
| M-03 | 公共函数必须有入参和返回值的类型定义 | 保证类型安全 |
| M-04 | 数据库操作必须在事务中执行（涉及多表时） | 保证数据一致性 |
| M-05 | 配置项必须集中管理 | 便于环境切换 |
<!-- REPEAT_START -->
| M-{{编号}} | {{必须行为}} | {{说明}} |
<!-- REPEAT_END -->

---

## 文档变更记录

| 版本 | 日期 | 变更内容 | 变更原因 |
|------|------|----------|----------|
| V1.0 | {{创建日期}} | 初始版本 | 开发规约初始定义 |
